{
  "name": "ha-health-dashboard",
  "version": "1.3.0",
  "description": "Health Hub — a twelve-page analytical health console for Home Assistant, built on the Claude Design 'Health Hub Light' spec and driven entirely by live HA entity data.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
